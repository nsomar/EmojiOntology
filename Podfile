# Uncomment this line to define a global platform for your project
# platform :ios, '8.0'
# Uncomment this line if you're using Swift
use_frameworks!

target 'EmojiOntology' do
end

target 'EmojiAnalyzer' do
  pod "SwiftCSV"
  pod "Colours"
  pod "Swiftline"
end

target 'EmojiAnalyzerTests' do
  pod "Quick"
  pod "Nimble"
end
